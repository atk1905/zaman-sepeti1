import { Menu, ShoppingBasket, X } from 'lucide-react';
import { useState } from 'react';
import { go } from '../lib/utils';
import { Button } from './ui';
const links=[['/ilanlar','Sıcak Talepler'],['/cozum-verenler','Çözüm Verenler'],['/talep-olustur','Talep Oluştur'],['/bildirimler','Bildirimler'],['/giris','Giriş'],['/kayit','Kayıt Ol']];
export function Header(){ const [open,setOpen]=useState(false); const nav=<>{links.map(([href,label])=><button key={href} onClick={()=>{go(href);setOpen(false)}} className="text-sm font-semibold text-zs-primary hover:text-zs-accent">{label}</button>)}</>; return <header className="sticky top-0 z-40 border-b border-zs-primary/10 bg-zs-bg/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between px-4 py-4"><button onClick={()=>go('/')} className="flex items-center gap-2 text-zs-primary"><span className="grid h-10 w-10 place-items-center rounded-xl bg-zs-primary text-white"><ShoppingBasket size={21}/></span><span className="font-serif text-2xl font-bold">ZamanSepeti</span></button><nav className="hidden items-center gap-6 md:flex">{nav}</nav><Button className="md:hidden" variant="secondary" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</Button></div>{open&&<nav className="grid gap-3 border-t border-zs-primary/10 px-4 pb-4 md:hidden">{nav}</nav>}</header> }
