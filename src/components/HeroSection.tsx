import { ArrowRight, CheckCircle2, Clock3, WalletCards } from 'lucide-react';
import { go } from '../lib/utils';
import { Button } from './ui';
export function HeroSection(){ return <section className="mx-auto grid max-w-7xl items-center gap-8 px-4 py-14 lg:grid-cols-[1.1fr_.9fr]"><div><span className="inline-flex rounded-full bg-zs-accent/15 px-4 py-2 text-sm font-bold text-zs-primary">Türkiye’nin ilk akıllı iş pazaryeri</span><h1 className="mt-6 font-serif text-5xl font-bold leading-tight text-zs-primary md:text-7xl">Zekan var, zamanın yok</h1><p className="mt-5 max-w-2xl text-xl text-zs-muted">Birinin yapay zekayla 30 dakikada yapacağı işi haftalarca bekleme.</p><div className="mt-6 flex flex-wrap gap-3 text-sm font-semibold text-zs-primary"><span className="flex items-center gap-2"><CheckCircle2 size={18}/>Ücretsiz başla</span><span className="flex items-center gap-2"><WalletCards size={18}/>Komisyon yok</span><span className="flex items-center gap-2"><Clock3 size={18}/>7 günde sonuç</span></div><div className="mt-8 flex flex-wrap gap-3"><Button onClick={()=>go('/talep-olustur')}>Talep Oluştur <ArrowRight size={18}/></Button><Button variant="secondary" onClick={()=>go('/ilanlar')}>Sıcak Talepleri Gör</Button></div></div><div className="rounded-[28px] border border-zs-primary/10 bg-white p-6 shadow-soft"><p className="text-sm font-bold text-zs-accent">Nasıl çalışır?</p>{['Talebini ve bütçeni yaz','Çözüm verenler teklif versin','En uygun teklifi kabul et','Ödeme MVP’de site dışında ilerler'].map((x,i)=><div key={x} className="mt-4 flex gap-4 rounded-xl bg-zs-bg p-4"><span className="grid h-8 w-8 shrink-0 place-items-center rounded-full bg-zs-primary text-white">{i+1}</span><p className="font-semibold">{x}</p></div>)}</div></section> }
